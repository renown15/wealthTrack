---
phase: 01-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
autonomous: true

must_haves:
  truths:
    - "Alembic is initialized with async template in backend/alembic/"
    - "Alembic env.py imports all models from app.models"
    - "Alembic configured to use postgresql+asyncpg:// driver"
  artifacts:
    - path: "backend/alembic.ini"
      provides: "Alembic configuration"
    - path: "backend/alembic/env.py"
      provides: "Async migration environment"
      contains: "async_engine_from_config"
---

<objective>
Initialize Alembic migration system with async PostgreSQL support.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Alembic with async template</name>
  <files>
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
  </files>
  <action>
1. cd backend && alembic init -t async alembic
2. Update env.py to import Base and models, configure async engine
  </action>
  <verify>
1. ls backend/alembic/ shows env.py, versions/
2. grep -q "async_engine_from_config" backend/alembic/env.py
  </verify>
  <done>Alembic initialized with async template</done>
</task>

</tasks>

<output>
Create .planning/phases/01-database-foundation/01-01-SUMMARY.md
</output>
