---
phase: 01-database-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic.ini
  - backend/alembic/env.py
  - backend/alembic/script.py.mako
  - backend/alembic/versions/001_create_users_table.py
  - backend/app/config.py
autonomous: true

must_haves:
  truths:
    - "Alembic is initialized with async template in backend/alembic/"
    - "Running `alembic upgrade head` creates users table in PostgreSQL"
    - "Alembic env.py imports all models from app.models"
  artifacts:
    - path: "backend/alembic.ini"
      provides: "Alembic configuration with async driver URL"
      contains: "postgresql+asyncpg://"
    - path: "backend/alembic/env.py"
      provides: "Async migration environment"
      contains: "async_engine_from_config"
    - path: "backend/alembic/versions/001_create_users_table.py"
      provides: "Initial migration for existing User model"
      contains: "create_table"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/database.py"
      via: "Base.metadata import"
      pattern: "from app.database import Base"
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "model imports for autogenerate"
      pattern: "from app.models import"
---

<objective>
Initialize Alembic migration system with async PostgreSQL support and create initial migration for existing User model.

Purpose: Establishes the migration infrastructure required for all subsequent database changes. Without Alembic, we cannot create or version database schema.

Output: Working Alembic setup that can apply migrations to PostgreSQL via asyncpg driver.
</objective>

<execution_context>
@/Users/marklewis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/marklewis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-database-foundation/01-RESEARCH.md

# Existing files to understand
@backend/app/database.py
@backend/app/models/user.py
@backend/app/models/__init__.py
@backend/app/config.py
@docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Alembic with async template</name>
  <files>
    backend/alembic.ini
    backend/alembic/env.py
    backend/alembic/script.py.mako
    backend/alembic/README
  </files>
  <action>
Initialize Alembic in the backend directory using the async template:

1. Change to backend directory and run: `alembic init -t async alembic`

2. Update `backend/alembic.ini`:
   - Set `sqlalchemy.url` to use environment variable: `sqlalchemy.url = postgresql+asyncpg://%(DB_USER)s:%(DB_PASS)s@%(DB_HOST)s:%(DB_PORT)s/%(DB_NAME)s`
   - OR configure to read from settings (preferred): Leave URL empty and configure in env.py

3. Update `backend/alembic/env.py` to:
   - Import Base from app.database
   - Import all models via `from app.models import *`
   - Set `target_metadata = Base.metadata`
   - Configure `compare_type=True` and `compare_server_default=True` in context.configure()
   - Read database URL from app.config.settings.database_url
   - Handle both online and offline modes properly

Key pattern from RESEARCH.md:
```python
from app.database import Base
from app.models import *  # Import all models for autogenerate

target_metadata = Base.metadata

def do_run_migrations(connection):
    context.configure(
        connection=connection,
        target_metadata=target_metadata,
        compare_type=True,
        compare_server_default=True,
    )
```

Ensure the async migration runner uses `async_engine_from_config` with `poolclass=pool.NullPool`.
  </action>
  <verify>
1. `ls backend/alembic/` shows env.py, script.py.mako, versions/, README
2. `grep -q "async_engine_from_config" backend/alembic/env.py` succeeds
3. `grep -q "from app.models import" backend/alembic/env.py` succeeds
  </verify>
  <done>Alembic initialized with async template, env.py configured to import all models and use async PostgreSQL driver</done>
</task>

<task type="auto">
  <name>Task 2: Update config for async database URL</name>
  <files>
    backend/app/config.py
  </files>
  <action>
Ensure `backend/app/config.py` provides the database URL with the asyncpg driver prefix.

The config should:
1. Read DATABASE_URL from environment
2. Ensure the URL uses `postgresql+asyncpg://` prefix (not just `postgresql://`)

If the current implementation uses plain `postgresql://`, update to transform it:
```python
@property
def database_url(self) -> str:
    url = self.database_url_raw
    # Ensure async driver is used
    if url.startswith("postgresql://"):
        return url.replace("postgresql://", "postgresql+asyncpg://", 1)
    return url
```

Also verify docker-compose.yml uses the correct format. The DATABASE_URL in docker-compose currently uses `postgresql://` - this is fine as the config.py transforms it.

IMPORTANT: Do NOT break existing functionality. The current app already works with asyncpg (per STACK.md), so verify the existing config before making changes.
  </action>
  <verify>
1. `grep -q "asyncpg" backend/app/config.py` shows async driver handling
2. `python -c "from app.config import settings; print(settings.database_url)"` outputs URL with `postgresql+asyncpg://`
  </verify>
  <done>Config returns database URL with postgresql+asyncpg:// driver prefix</done>
</task>

<task type="auto">
  <name>Task 3: Create initial migration for User model</name>
  <files>
    backend/alembic/versions/001_create_users_table.py
  </files>
  <action>
Create the first migration for the existing User model:

1. Start PostgreSQL: `docker-compose up -d db`
2. Wait for health check: `docker-compose exec db pg_isready -U wealthtrack`
3. Generate migration: `cd backend && alembic revision --autogenerate -m "create users table"`
4. Rename the generated file to `001_create_users_table.py` for clarity (update revision ID reference if needed)

Review the generated migration to ensure it includes:
- `op.create_table('users', ...)` with all columns from User model:
  - id (Integer, primary_key)
  - email (String(255), unique, not null)
  - username (String(50), unique, not null)
  - hashed_password (String(255), not null)
  - full_name (String(255), nullable)
  - is_active (Boolean, default True)
  - is_verified (Boolean, default False)
  - created_at (DateTime, not null)
  - updated_at (DateTime, not null)
- Index creation for email and username
- Proper downgrade with `op.drop_table('users')`

Apply the migration: `alembic upgrade head`

Verify the table exists: `docker-compose exec db psql -U wealthtrack -c "\d users"`
  </action>
  <verify>
1. `ls backend/alembic/versions/*.py` shows migration file
2. `cd backend && alembic current` shows current revision
3. `docker-compose exec db psql -U wealthtrack -c "\d users"` shows users table structure
  </verify>
  <done>Initial migration created and applied, users table exists in PostgreSQL with correct schema</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `docker-compose up -d db` - PostgreSQL running
2. `cd backend && alembic current` - Shows applied revision
3. `docker-compose exec db psql -U wealthtrack -c "SELECT table_name FROM information_schema.tables WHERE table_schema='public';"` - Shows 'users' and 'alembic_version' tables
4. Backend can start: `cd backend && python -c "from app.database import engine; print('OK')"`
</verification>

<success_criteria>
1. Alembic initialized with async template in backend/alembic/
2. env.py imports all models and uses async engine
3. First migration creates users table matching User model
4. Migration applied successfully to PostgreSQL
5. No breaking changes to existing auth functionality
</success_criteria>

<output>
After completion, create `.planning/phases/01-database-foundation/01-01-SUMMARY.md`
</output>
