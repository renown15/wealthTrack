# Local production frontend (nginx) environment
# Used by 'make local-prod' to run the built frontend via Docker
# Port is different from dev (3001) to avoid conflicts with Vite dev server

FRONTEND_PORT=3002
FRONTEND_CONTAINER=wealthtrack-frontend-prod
VITE_API_URL=http://localhost:8000
