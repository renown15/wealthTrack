---
phase: 01-database-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/alembic/versions/001_create_tables.py
  - backend/alembic/versions/002_seed_reference_data.py
autonomous: true

must_haves:
  truths:
    - "Running alembic upgrade head creates reference_data and user_profiles tables"
    - "ReferenceData query returns UK account types with correct sortindex"
    - "Database uses single reference_data table with 'class' column"
  artifacts:
    - path: "backend/alembic/versions/001_create_tables.py"
      provides: "Migration for reference_data and user_profiles tables"
      contains: "create_table"
    - path: "backend/alembic/versions/002_seed_reference_data.py"
      provides: "Seed data for all reference types"
      contains: "bulk_insert"
---

<objective>
Create and apply migrations for database tables and seed reference data.
Completes DB-01 (PostgreSQL schema), DB-02 (ReferenceData table), DB-03 (seed UK account types).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Generate migrations for models</name>
  <files>
    backend/alembic/versions/001_create_tables.py
  </files>
  <action>
1. docker-compose up -d db
2. cd backend && alembic revision --autogenerate -m "create tables"
3. Verify migration creates reference_data and user_profiles tables
  </action>
  <verify>
1. ls backend/alembic/versions/*.py shows migration files
2. grep -q "reference_data" backend/alembic/versions/*.py
  </verify>
  <done>Migrations generated for all tables</done>
</task>

<task type="auto">
  <name>Task 2: Create seed migration for reference data</name>
  <files>
    backend/alembic/versions/002_seed_reference_data.py
  </files>
  <action>
Create data migration seeding all ReferenceData class types:

account_type: SAVINGS, CURRENT, CASH_ISA, STOCKS_ISA, LIFETIME_ISA, SIPP, PREMIUM_BONDS
account_status: ACTIVE, CLOSED, DORMANT, PENDING
account_event_type: BALANCE_UPDATE, INTEREST, DIVIDEND, DEPOSIT, WITHDRAWAL
credential_type: USERNAME, PASSWORD, PIN, SECURITY_QUESTION, MEMORABLE_WORD
user_profile_type: STANDARD, ADMIN
account_attribute_type: ACCOUNT_NUMBER, SORT_CODE, IBAN, NOTES

Use op.bulk_insert() with table/column pattern.
  </action>
  <verify>
1. grep -q "bulk_insert" backend/alembic/versions/*seed*.py
2. grep -q "account_type" backend/alembic/versions/*seed*.py
  </verify>
  <done>Seed migration created with all reference data types</done>
</task>

<task type="auto">
  <name>Task 3: Apply migrations and verify</name>
  <action>
1. cd backend && alembic upgrade head
2. Verify: docker-compose exec db psql -U wealthtrack -c "\dt"
3. Verify: docker-compose exec db psql -U wealthtrack -c "SELECT class, key, referencevalue FROM reference_data WHERE class='account_type' ORDER BY sortindex;"
  </action>
  <verify>
1. alembic current shows head revision
2. SELECT COUNT(*) FROM reference_data returns expected count
  </verify>
  <done>Migrations applied, all tables created, reference data seeded</done>
</task>

</tasks>

<output>
Create .planning/phases/01-database-foundation/01-03-SUMMARY.md
</output>
