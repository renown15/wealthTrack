---
phase: 01-database-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/models/reference_data.py
  - backend/app/models/user_profile.py
  - backend/app/models/__init__.py
autonomous: true

must_haves:
  truths:
    - "ReferenceData model exists with columns: id, class_, key, referencevalue, sortindex"
    - "UserProfile model exists with columns: id, firstname, surname, emailaddress, profile (JSON), typeid (FK), password"
    - "Models use single ReferenceData table with 'class' column for different types"
    - "Models are exported from app.models for Alembic autogenerate"
  artifacts:
    - path: "backend/app/models/reference_data.py"
      provides: "Single ReferenceData lookup table model"
      contains: "class ReferenceData"
    - path: "backend/app/models/user_profile.py"
      provides: "UserProfile model per spec"
      contains: "class UserProfile"
---

<objective>
Create SQLAlchemy models matching the WealthTrack Data Model specification.

Key design from spec:
- Single ReferenceData table with 'class' column (NOT separate tables per type)
- UserProfile table with: id, firstname, surname, emailaddress, profile JSON, typeid FK, password
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create ReferenceData model</name>
  <files>
    backend/app/models/reference_data.py
  </files>
  <action>
Create single ReferenceData table per WealthTrack Data Model spec:
- id (primary key)
- class (String, indexed) - stores: account_type, account_status, credential_type, etc.
- key (String) - e.g., 'SAVINGS', 'CURRENT'
- referencevalue (String) - e.g., 'Savings Account'
- sortindex (Integer)
- created_at, updated_at

Note: 'class' is reserved in Python, use 'class_' with explicit column name.
  </action>
  <verify>
1. grep -q "class ReferenceData" backend/app/models/reference_data.py
2. grep -q 'class_' backend/app/models/reference_data.py
  </verify>
  <done>ReferenceData model created with single table design per spec</done>
</task>

<task type="auto">
  <name>Task 2: Create UserProfile model</name>
  <files>
    backend/app/models/user_profile.py
  </files>
  <action>
Create UserProfile per WealthTrack Data Model spec:
- id (primary key)
- firstname (String)
- surname (String)
- emailaddress (String, unique, indexed)
- profile (JSON, nullable)
- typeid (FK to reference_data)
- password (String, encrypted)
- created_at, updated_at
  </action>
  <verify>
1. grep -q "class UserProfile" backend/app/models/user_profile.py
2. grep -q "typeid" backend/app/models/user_profile.py
  </verify>
  <done>UserProfile model created matching spec</done>
</task>

<task type="auto">
  <name>Task 3: Export models in __init__.py</name>
  <files>
    backend/app/models/__init__.py
  </files>
  <action>
Export ReferenceData and UserProfile from app.models for Alembic autogenerate.
  </action>
  <verify>
1. grep -q "ReferenceData" backend/app/models/__init__.py
2. grep -q "UserProfile" backend/app/models/__init__.py
  </verify>
  <done>All models exported</done>
</task>

</tasks>

<output>
Create .planning/phases/01-database-foundation/01-02-SUMMARY.md
</output>
